#!/bin/sh
echo "Running tests before push..."
npx vitest run tests/unit/ --reporter=verbose 2>&1
if [ $? -ne 0 ]; then
  echo ""
  echo "Tests failed. Push aborted."
  exit 1
fi
echo "All tests passed."
